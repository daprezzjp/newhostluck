{{> html_head}}
{{> navbar}}

<div class="container">
  <h2>Event Info</h2>
  {{#addhosts}}
  {{#if @last}}
      <h3> Event name: {{eventname}}</h3>
      <h3> Day: {{date}}</h3>
      <h3> Time: {{time}}</h3>


  <h3> Hosted Food: {{foodname}}</h3>
  {{/if}}
  {{/addhosts}}

  {{#addresses}}
  <h3>{{#if @last}}Address: {{street}} {{city}}, {{state}} <br />{{zip}}</a>{{/if}}</h3>
  {{/addresses}}


  {{#addhosts}}
  {{#if @last}}
  <a href="/recipe/{{id}}" class="btn btn-lg btn-default col-sm-6">Recipe</a>
{{/if}}
{{/addhosts}}
  <a href="/cancelled" class="btn btn-lg btn-default col-sm-6">Cancel Event</a>
  <div role="collapsible">
    <h1>My Ingredients</h1>
    <fieldset data-role="controlgroup">
      <input type="checkbox" name="checkbox-1" id="checkbox-1" class="custom" />
      <label for="checkbox-1">Onions</label>
      <input type="checkbox" name="checkbox-2" id="checkbox-2" class="custom" />
      <label for="checkbox-2">Beans</label>
      <input type="checkbox" name="checkbox-3" id="checkbox-3" class="custom" />
      <label for="checkbox-3">Corn On The Cob</label>
    </fieldset>
  </div>
</div>

</body>
{{> footer_back_home}}
</html>
