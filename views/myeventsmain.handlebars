{{> html_head}}
{{> navbar}}

<body>
<div class="container">
  <a href="#" class="btn btn-danger">Delete an event</a>
</div>
  <div class="container">
    <h2>Host:</h2>
    {{#addhosts}}
        <a href="/eventinfohost/{{_id}}" class="btn btn-lg btn-default btn-block"> {{eventname}}</a>
      <br>
    {{/addhosts}}

    <h2>Attend:</h2>
    {{#each guestevents}}
        <a href="#" class="btn btn-lg btn-default btn-block"> {{eventname}}</a>
      <br>
    {{/each}}
  </div>

</body>

</html>
