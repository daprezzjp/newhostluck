{{> html_head}}
{{> navbar}}

<body>
<div class="container">
  <a href="#" class="btn btn-danger">Delete an event</a>
</div>
  <div class="container">
    <h2>Host:</h2>
    {{#addhosts}}
    <div class="panel-group">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h4 class="panel-title">
            <a data-toggle="collapse" href="#collapse{{_id}}" class="btn btn-lg btn-default btn-block"> {{eventname}}</a>
          </h4>
        </div>
        <div id="collapse{{_id}}" class="panel-collapse collapse">
          <h2>{{foodname}} </h2>
          <h5>Ingredients:</h5>
          <ul class="list-group">
            <li class="list-group-item">{{recipe1}}</li>
            <li class="list-group-item">{{recipe2}}</li>
            <li class="list-group-item">{{recipe3}}</li>
            <li class="list-group-item">{{recipe4}}</li>
            <li class="list-group-item">{{recipe5}}</li>
          </ul>
        </div>
      </div>
    </div>
      <br>
    {{/addhosts}}

    <h2>Attend:</h2>
    {{#addhosts}}
    <div class="panel-group">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h4 class="panel-title">
            <a data-toggle="collapse" href="#collapse{{_id}}" class="btn btn-lg btn-default btn-block"> {{eventname}}</a>
          </h4>
        </div>
        <div id="collapse{{_id}}" class="panel-collapse collapse">
          <h4>{{_id}}</h4>
        </div>
      </div>
    </div>
      <br>
    {{/addhosts}}
  </div>

</body>

</html>
