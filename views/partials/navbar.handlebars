<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
        <p>Hostluck</p>
        {{#if user}}
        <p class="navbar-text navbar-right"><a href="#" class="navbar-link">Signed in as {{user.username}}</a></p>
        {{/if}}
      </a>
    </div>
  </div>
</nav>
